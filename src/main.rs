mod map;
mod stringes;
mod vetor;

use map as m;
use stringes as s;
use vetor as v;

fn main() {
    v::vetores();
    println!("======================");
    s::stringes();
    println!("======================");
    m::map();
}
